<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chemistry">

    <uses-permission android:name="android.permission.INTERNET" />

    <application

        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="activities.SplashScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="activities.ChemItemActivity" />
        <!--android:theme="@style/NoActionBar"-->
        <activity android:name="activities.ConversionActivity" />
        <activity android:name="activities.HelpActivity" />
        <activity
            android:name="activities.PeriodicTableActivity"
            android:screenOrientation="landscape" />
        <activity android:name="activities.CompoundResults" />
        <activity android:name="activities.MoleActivity" />
        <activity android:name="activities.ContactActivity" />
        <activity
            android:name="activities.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAINACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>