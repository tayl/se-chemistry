<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.daniel.chemistryhandbook">
    <uses-sdk android:minSdkVersion="15" />
    <supports-screens android:largeScreens="true" android:resizeable="true" />

    <application

        android:allowBackup="true" android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashScreen" android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ChemItemActivity" />

        <activity android:name=".ConversionActivity" />

        <activity android:name=".HelpActivity" />

        <activity
            android:name=".PeriodicTableActivity"
            android:screenOrientation="landscape" android:theme="@style/NoActionBar" />

        <activity android:name=".CompoundResults"
            android:label="@string/title_activity_main"
            android:theme="@style/NoActionBar"></activity>

        <activity android:name=".MoleActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/NoActionBar"></activity>

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAINACTIVITY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
